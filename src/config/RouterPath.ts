export enum RouterPath {
    ROOT = '/',
    PAGE_NOT_FOUND = '*',
    ABOUT = 'about',

    //intro
    REACT_TRAITS = 'intro/react-traits',

    // routing
    DYNAMIC_ROUTES = 'routing/dynamic-routes',
}
